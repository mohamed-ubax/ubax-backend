package com.africa.ubaxplatform.ticketing.codeList;

public enum CostImputedTo {
    /** Coût à la charge du propriétaire (vétusté, défaut structurel). */
    OWNER,
    /** Coût à la charge du locataire (mauvais usage, dégradation). */
    TENANT,
    /** Coût partagé entre propriétaire et locataire. */
    SHARED
}