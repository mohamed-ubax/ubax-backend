server:
  port: ${SERVER_PORT:9999}
  servlet:
    context-path: ${SERVER_SERVLET_CONTEXT_PATH:/api}
  max-http-request-header-size: 126KB
  forward-headers-strategy: framework

spring:
  application:
    name: ubax-platform

  # ─── Multipart (upload fichiers) ─────────────────────────────────
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # ─── PostgreSQL ──────────────────────────────────────────────────
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:db-ubax}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:ubax123}

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: administrative
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false

  flyway:
    enabled: true
    default-schema: administrative
    schemas: administrative
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    out-of-order: false

  # ─── Keycloak OAuth2 Resource Server ─────────────────────────────
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/ubax-plateform}
          jwk-set-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/ubax-plateform}/protocol/openid-connect/certs

  # ─── JavaMail – SMTP ─────────────────────────────────────────────
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:no-reply@ubax.io}
    password: ${MAIL_PASSWORD:lljv venq dmmm rhgg}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false

  # ─── Thymeleaf ───────────────────────────────────────────────────
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

# ─── Keycloak Client ─────────────────────────────────────────────
keycloak:
  realm: ${KEYCLOAK_REALM:ubax-plateform}
  auth-server-url: ${KEYCLOAK_URL:http://localhost:8180}
  client-id: ${KEYCLOAK_CLIENT_ID:ubax-client}
  client-secret: ${KEYCLOAK_CLIENT_SECRET:ky2NlSdFT8H7KuYA8I3weY9tjQTXLtkQ}
  issuer-uri: ${keycloak.auth-server-url}/realms/${keycloak.realm}

# ─── MinIO (Object Storage) ──────────────────────────────────────
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin123}
  buckets:
    users-avatars,
    agencies-logos,
    properties-media,
    property-documents,
    tenant-documents,
    documents-generated,
    ticket-attachments

# ─── Swagger / OpenAPI ───────────────────────────────────────────
springdoc:
  swagger-ui:
    path: ${SPRINGDOC_SWAGGER_UI_PATH:/swagger-ui.html}
    oauth:
      client-id: ${keycloak.client-id}
      realm: ${keycloak.realm}
  api-docs:
    path: ${SPRINGDOC_API_DOCS_PATH:/api-docs}

# ─── Actuator / Monitoring ─────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: when-authorized

# ─── Ubax Application ────────────────────────────────────────────
ubax:
  security:
    enabled: ${SECURITY_ENABLED:true}
  endpoints:
    frontend: ${FRONTEND_URL:http://localhost:3000}
  mail:
    from: ${MAIL_FROM:no-reply@ubax.io}
    from-name: ${MAIL_FROM_NAME:UBAX Platform}

# ─── SMS – LAfricaMobile (LAMPUSH) ────────────────────────────────
lam:
  sms:
    api-url: ${LAM_SMS_URL:https://lamsms.lafricamobile.com/api}
    account-id: ${LAM_ACCOUNT_ID:UBAX_COTE_DIVOIRE_01}
    password: ${LAM_PASSWORD:zHI18WaOevNmLE5}
    sender: ${LAM_SENDER:UBAX}