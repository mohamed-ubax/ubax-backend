package com.africa.ubaxplatform.auth.codeList;

/**
 * Mode d'affichage par défaut des résultats de recherche immobilière.
 */
public enum DisplayMode {
    /** Liste de résultats uniquement. */
    LIST,
    /** Carte interactive uniquement. */
    MAP,
    /** Liste + carte côte à côte (mode desktop recommandé selon le CDC). */
    SPLIT
}