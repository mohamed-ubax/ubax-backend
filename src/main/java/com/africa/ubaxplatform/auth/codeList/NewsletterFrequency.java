package com.africa.ubaxplatform.auth.codeList;

/**
 * Fréquence de réception de la newsletter de la plateforme UBAX.
 */
public enum NewsletterFrequency {
    /** Newsletter envoyée chaque semaine. */
    WEEKLY,
    /** Newsletter envoyée chaque mois. */
    MONTHLY
}