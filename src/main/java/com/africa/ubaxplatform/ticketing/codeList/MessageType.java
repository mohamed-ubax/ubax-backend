package com.africa.ubaxplatform.ticketing.codeList;

/** Visibilité d'un message dans le fil de discussion d'un ticket. */
public enum MessageType {
  /** Visible par toutes les parties (locataire, agent, propriétaire). */
  PUBLIC,
  /** Note interne visible uniquement par l'équipe SAV. */
  INTERNAL
}
